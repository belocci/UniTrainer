# Install

This repository contains source code for Uni Trainer.

## Windows (recommended)
- Download the latest Windows build from GitHub Releases.
- Extract the zip and run `UniTrainer.exe`.

## From source (developers)
- Install Node.js (LTS) and Python 3.8+.
- Run `npm install`.
- Run `npm run dev`.
