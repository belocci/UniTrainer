# Cloud Training Workflow

## Complete End-to-End Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER INITIATES                            │
│              Click "Start Training" (Cloud Mode)                 │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  FEATURE 1: INSTANCE IP RETRIEVAL                                │
├─────────────────────────────────────────────────────────────────┤
│  1. Launch Instance via CanopyWave API                           │
│     POST /instance-operations/launch                             │
│     → Returns: { id: "inst-123", status: "BUILD" }              │
│                                                                   │
│  2. Poll Instance Status (every 5 seconds)                       │
│     GET /instances/inst-123?project=X&region=Y                   │
│     → Wait for: status = "ACTIVE"                                │
│                                                                   │
│  3. Extract IP Address                                           │
│     → Check: ip, floating_ip, public_ip, addresses               │
│     → Result: "203.0.113.45"                                     │
│                                                                   │
│  4. Wait 30 seconds for SSH daemon                               │
│     → SSH service ready                                          │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  SSH CONNECTION                                                   │
├─────────────────────────────────────────────────────────────────┤
│  Connect to: ubuntu@203.0.113.45                                 │
│  Auth: Password-based                                            │
│  Retries: 5 attempts with 10s delay                              │
│  → Connection established                                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  FEATURE 2: REMOTE TRAINING SCRIPT EXECUTION                     │
├─────────────────────────────────────────────────────────────────┤
│  STEP 1: Setup Environment                                       │
│  ├─ sudo apt-get update                                          │
│  ├─ sudo apt-get install python3 python3-pip                     │
│  ├─ pip3 install torch torchvision (CUDA 11.8)                   │
│  ├─ pip3 install ultralytics                                     │
│  ├─ pip3 install scikit-learn xgboost lightgbm                   │
│  └─ mkdir -p ~/training/{dataset,output}                         │
│                                                                   │
│  STEP 2: Upload Dataset                                          │
│  ├─ SFTP: Local → ~/training/dataset/                            │
│  └─ Upload: images/, labels/, data.yaml                          │
│                                                                   │
│  STEP 3: Generate & Upload Training Script                       │
│  ├─ Generate: train.py (YOLO/PyTorch/etc)                        │
│  └─ SFTP: train.py → ~/training/train.py                         │
│                                                                   │
│  STEP 4: Execute Training                                        │
│  └─ SSH: cd ~/training && python3 train.py                       │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  FEATURE 3: PROGRESS STREAMING OVER SSH                          │
├─────────────────────────────────────────────────────────────────┤
│  SSH Stream (Real-time)                                          │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ REMOTE OUTPUT:                                            │  │
│  │ Epoch: 1/10  Loss: 0.543  mAP: 0.234                     │  │
│  │ Epoch: 2/10  Loss: 0.421  mAP: 0.456                     │  │
│  │ Epoch: 3/10  Loss: 0.356  mAP: 0.612                     │  │
│  │ ...                                                       │  │
│  └───────────────────────────────────────────────────────────┘  │
│                      │                                            │
│                      ▼                                            │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ PARSE PROGRESS:                                           │  │
│  │ • Regex: /Epoch[:\s]+(\d+)\/(\d+)/                       │  │
│  │ • JSON: {"type": "progress", "data": {...}}              │  │
│  │ • Extract: epoch, loss, metrics                          │  │
│  └───────────────────────────────────────────────────────────┘  │
│                      │                                            │
│                      ▼                                            │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ SEND TO UI:                                               │  │
│  │ mainWindow.webContents.send('training-progress', {       │  │
│  │   epoch: 3,                                               │  │
│  │   total_epochs: 10,                                       │  │
│  │   progress: 0.3,                                          │  │
│  │   metrics: { loss: 0.356, mAP: 0.612 }                   │  │
│  │ })                                                        │  │
│  └───────────────────────────────────────────────────────────┘  │
│                      │                                            │
│                      ▼                                            │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ UI UPDATES:                                               │  │
│  │ [████████░░░░░░░░░░] 30% Complete                        │  │
│  │ Epoch 3/10 • Loss: 0.356 • mAP: 0.612                    │  │
│  └───────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  TRAINING COMPLETES                                              │
├─────────────────────────────────────────────────────────────────┤
│  Remote: Training finished!                                      │
│  Model saved: ~/training/output/training_run/weights/best.pt    │
│  Exit code: 0                                                    │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  FEATURE 4: MODEL DOWNLOAD AUTOMATION                            │
├─────────────────────────────────────────────────────────────────┤
│  STEP 1: Determine Model Location                                │
│  ├─ Framework: YOLO                                              │
│  ├─ Primary: ~/training/output/training_run/weights/best.pt     │
│  └─ Fallbacks: [last.pt, model.pt, best.pth]                    │
│                                                                   │
│  STEP 2: Create Local Directory                                  │
│  └─ mkdir ~/Documents/UniTrainer/models                          │
│                                                                   │
│  STEP 3: Download via SFTP                                       │
│  ├─ Remote: ~/training/output/.../best.pt                        │
│  ├─ Local: ~/Documents/UniTrainer/models/                        │
│  └─ Filename: model_yolo_2026-01-16T14-30-45.pt                 │
│                                                                   │
│  STEP 4: Verify Download                                         │
│  ├─ Check file exists                                            │
│  ├─ Check file size > 0                                          │
│  └─ Success! Model ready for use                                 │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  CLEANUP                                                          │
├─────────────────────────────────────────────────────────────────┤
│  1. Close SSH Connection                                         │
│  2. Terminate Cloud Instance                                     │
│     POST /instance-operations/terminate                          │
│     { id: "inst-123", project: "X", region: "Y" }               │
│  3. Remove temporary files                                       │
│  4. Clear training state                                         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                         SUCCESS!                                 │
├─────────────────────────────────────────────────────────────────┤
│  ✅ Model trained on cloud GPU                                   │
│  ✅ Model downloaded to local machine                            │
│  ✅ Cloud resources cleaned up                                   │
│  ✅ Ready for inference/deployment                               │
│                                                                   │
│  Result: {                                                       │
│    success: true,                                                │
│    modelPath: "~/Documents/UniTrainer/models/model_yolo_...pt", │
│    trainingResult: { epochs: 10, final_loss: 0.123 }            │
│  }                                                                │
└─────────────────────────────────────────────────────────────────┘
```

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────────────┐
│  ERROR OCCURS AT ANY STEP                                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  AUTOMATIC CLEANUP                                               │
├─────────────────────────────────────────────────────────────────┤
│  try {                                                           │
│    // Training workflow                                          │
│  } catch (error) {                                               │
│    ├─ Log error details                                          │
│    ├─ Close SSH connection                                       │
│    ├─ Terminate cloud instance                                   │
│    ├─ Remove temp files                                          │
│    └─ Send error to UI                                           │
│  }                                                                │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  USER NOTIFIED                                                   │
├─────────────────────────────────────────────────────────────────┤
│  ❌ Training failed: [Error message]                             │
│  💰 Instance terminated (no ongoing charges)                     │
│  🔄 Ready to retry                                               │
└─────────────────────────────────────────────────────────────────┘
```

## Timing Estimates

| Phase                    | Time Estimate      | Notes                          |
|--------------------------|-------------------|--------------------------------|
| Instance Launch          | 30-120 seconds    | Depends on GPU availability    |
| IP Retrieval             | 5-30 seconds      | Polling every 5 seconds        |
| SSH Connection           | 5-60 seconds      | Includes retries               |
| Environment Setup        | 3-10 minutes      | PyTorch download is largest    |
| Dataset Upload           | 1-30 minutes      | Depends on dataset size        |
| Training                 | Variable          | Depends on model/data/epochs   |
| Model Download           | 10-60 seconds     | Typical model: 5-50 MB         |
| Instance Termination     | 5-15 seconds      | API call + cleanup             |

**Total Overhead**: ~5-15 minutes (excluding actual training time)

## Key Implementation Files

```
uni-trainer/
├── cloud-training-handler.js       ← Main implementation (700 lines)
├── cloud-ssh-utils.js              ← SSH utilities (249 lines)
├── canopywave-api.js               ← API client (382 lines)
├── main.js                         ← IPC handlers (added 50 lines)
└── CLOUD_TRAINING_IMPLEMENTATION.md ← This documentation
```

## Usage Example

```javascript
// In renderer.js
const result = await ipcRenderer.invoke('start-cloud-training', apiKey, {
  // CanopyWave config
  project: 'my-ml-project',
  region: 'seq',
  flavor: 'H100-4',
  image: 'GPU-Ubuntu.22.04',
  password: 'SecurePass123!',
  
  // Training config
  datasetPath: 'C:\\Users\\vaugh\\datasets\\my-yolo-dataset',
  trainingSettings: {
    framework: 'yolo',
    modelVariant: 'yolov11n',
    epochs: 10,
    batchSize: 16,
    imageSize: 640
  }
});

if (result.success) {
  console.log('✅ Training complete!');
  console.log('📦 Model:', result.result.modelPath);
} else {
  console.error('❌ Error:', result.error);
}
```

## Next Steps

1. **Test with small dataset** (10-20 images, 2 epochs)
2. **Monitor first run** (check console logs)
3. **Verify model download** (file exists and size > 0)
4. **Scale up** (larger datasets, more epochs)
5. **Optimize** (compression, parallel uploads, etc.)
